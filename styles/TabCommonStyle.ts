import { StyleSheet } from 'react-native';

export const TabCommonStyle = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        padding: 8,
        gap: 8
    }
});
